package com.mctrack.common.model;

/**
 * Enum representing Bedrock device types.
 * Used when Floodgate is available to identify the device type of Bedrock players.
 */
public enum BedrockDevice {
    ANDROID,
    IOS,
    WINDOWS,
    XBOX,
    PLAYSTATION,
    SWITCH
}
